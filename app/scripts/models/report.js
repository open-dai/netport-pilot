// Report module
define([
    'jquery',
    'backbone'
],
function($, Backbone) {
    'use strict';
    // Default Model.
    var ReportModel = Backbone.Model.extend({

    });

    return ReportModel;

});