// Report module
define([
    'jquery',
    'backbone'
],
function($, Backbone) {
    'use strict';
    // Default Model.
    var TypesModel = Backbone.Model.extend({

    });

    return TypesModel;

});